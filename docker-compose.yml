services:
  spmg:
    build: .
    image: spmg:latest
    container_name: spmg
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - COOKIE_SECRET=changeme-in-production
      # - COLLECT_MS=5000
    volumes:
      - spmg-data:/app/data
      - spmg-videos:/app/public/videos
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  spmg-data:
  spmg-videos:
